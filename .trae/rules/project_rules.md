1. testing server is already running at "http://localhost:5080/anoop/web-skit/"
2. folder "./widgets" is for UI/UX scripts (html,css,js).
3. folder "./plugins" is for non UI/UX scripts (js), it is for more generic/agnostic scripts.
